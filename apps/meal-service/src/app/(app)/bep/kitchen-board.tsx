"use client";

import { ArrowLeft, BookOpen, ChefHat, Clock3, ShoppingBasket } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { KitchenCompletionDialog } from "./kitchen-completion-dialog";

type MenuItem = { name: string; dishName: string; grams: number | null };
type Meal = { id: string; code: string; name: string; planned: number | null; additions: number | null; total: number | null; items: MenuItem[]; status: string };
type Shopping = { foodId: string; foodName: string; edible: string; raw: string };

export function KitchenBoard({ eventId, mealName, serviceAt, meals, shopping }: { eventId: string; mealName: string; serviceAt: string; meals: Meal[]; shopping: Shopping[] }) {
  const [selected, setSelected] = useState<string | null>(null); const [now, setNow] = useState(0);
  useEffect(() => { const immediate = window.setTimeout(() => setNow(Date.now()), 0); const timer = window.setInterval(() => setNow(Date.now()), 30_000); return () => { window.clearTimeout(immediate); window.clearInterval(timer); }; }, []);
  const remaining = Math.max(0, new Date(serviceAt).getTime() - now); const hours = Math.floor(remaining / 3_600_000); const minutes = Math.ceil((remaining % 3_600_000) / 60_000);
  const active = meals.find((meal) => meal.id === selected) ?? null; const prepared = meals.length > 0 && meals.every((meal) => meal.status === "PREPARED");
  const total = useMemo(() => meals.some((meal) => meal.total !== null) ? meals.reduce((sum, meal) => sum + (meal.total ?? 0), 0) : null, [meals]);
  return <div className="kitchen-operation-grid"><section className="kitchen-operation-main"><header><div><span><ChefHat/> Số suất & tiến độ</span><h2>{mealName}</h2></div><div className="kitchen-clock"><Clock3/><span>Thời gian chuẩn bị còn lại</span><strong>{remaining ? `${hours ? `${hours} giờ ` : ""}${minutes} phút` : "Đến giờ phục vụ"}</strong></div><b>{total === null ? "—" : `${total} suất`}</b></header><div className="kitchen-operation-scroll"><table><thead><tr><th>Chế độ ăn</th><th>Thực đơn · khối lượng mỗi suất</th><th>Suất báo</th><th>Bổ sung</th><th>Tổng</th></tr></thead><tbody>{meals.map((meal) => <tr key={meal.id} onClick={() => setSelected(meal.id)} tabIndex={0} onKeyDown={(event) => { if (event.key === "Enter") setSelected(meal.id); }}><th><button type="button"><BookOpen/><span><b translate="no">{meal.code}</b><small>{meal.name}</small></span></button></th><td>{meal.items.length ? [...new Set(meal.items.map((item) => item.dishName))].map((dish) => <div className="kitchen-dish-line" key={dish}><strong>{dish}</strong><small>{meal.items.filter((item) => item.dishName === dish).map((item) => `${item.name} ${item.grams === null ? "—" : `${item.grams} g`}`).join(" · ")}</small></div>) : "— · Chưa có thực đơn duyệt"}</td><td>{meal.planned ?? "—"}</td><td>{meal.additions ? `+${meal.additions}` : "—"}</td><td><b>{meal.total ?? "—"}</b></td></tr>)}</tbody><tfoot><tr><th colSpan={2}>Tổng suất toàn bữa</th><td>{meals.some((meal) => meal.planned !== null) ? meals.reduce((sum, meal) => sum + (meal.planned ?? 0), 0) : "—"}</td><td>{meals.some((meal) => meal.additions) ? `+${meals.reduce((sum, meal) => sum + (meal.additions ?? 0), 0)}` : "—"}</td><td>{total ?? "—"}</td></tr></tfoot></table></div><footer><KitchenCompletionDialog eventId={eventId} meals={meals.map(({ id, code, name }) => ({ id, code, name }))} prepared={prepared}/></footer></section><aside className="kitchen-operation-side">{active ? <><header><button type="button" onClick={() => setSelected(null)} aria-label="Trở lại danh sách đi chợ"><ArrowLeft/></button><div><span>Chi tiết mã</span><h2 translate="no">{active.code}</h2><small>{active.name}</small></div></header><div className="kitchen-side-scroll"><h3>Thực đơn và định lượng</h3>{active.items.length ? [...new Set(active.items.map((item) => item.dishName))].map((dish) => <article key={dish}><strong>{dish}</strong>{active.items.filter((item) => item.dishName === dish).map((item, index) => <p key={`${item.name}-${index}`}><span>{item.name}</span><b>{item.grams === null ? "—" : `${item.grams} g/suất`}</b></p>)}</article>) : <p>— · Chưa có dữ liệu thực đơn.</p>}</div></> : <><header><ShoppingBasket/><div><span>Đi chợ dự kiến</span><h2>Thực phẩm cần dùng</h2><small>Cho toàn bộ bữa</small></div></header><div className="kitchen-side-scroll">{shopping.length ? shopping.map((item) => <article key={item.foodId}><strong>{item.foodName}</strong><p><span>Ăn được</span><b>{item.edible}</b></p><p><span>Cần mua/xuất</span><b>{item.raw}</b></p></article>) : <p>— · Chưa đủ dữ liệu để tính.</p>}</div></>}</aside></div>;
}
