{
  "name": "suat-an-benh-vien",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev -w @suat-an/meal-service",
    "db:migrate": "npm run db:migrate -w @suat-an/meal-service",
    "db:seed": "npm run db:seed -w @suat-an/meal-service"
  },
  "engines": { "node": ">=22" }
}
